#include <iostream>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--)
	{
	    char a,b,c;
	    cin>>a>>b>>c;
	    char x,y;
	    cin>>x>>y;
	    if(a == x || a == y)
	        cout<<a<<"\n";
	    else if(b == x || b == y)
	        cout<<b<<"\n";
	    else
	        cout<<c<<"\n";
	}
	return 0;
}
